// the parent has children
// the children are 2n and 2n+1
// to find the math.floor(n/2)
// arr[i] = arr.pop()

function insert(arr, val){
    arr.push(val)
    var temp = 0
    while(true){
        if (arr[arr.length-1] < arr[Math.floor((arr.length-1)/2)]){
            arr[]
        }
    }
}